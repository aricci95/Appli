{% extends 'base.html.twig' %}

{% block body %}
    <ul>
        {% for user in users %}
            <li>{{ user.userLogin }} <a href="{{ path('user_delete' , {id : user.userId}) }}">x</a></li>
        {% endfor %}
    </ul>
{% endblock %}
